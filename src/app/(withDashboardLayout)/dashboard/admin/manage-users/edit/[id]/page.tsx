const UpdateUser = ({ params }: { params: { id: string } }) => {
  const id = params?.id;

  return (
    <div>
      <h1 className="text-3xl text-center">Update user page id: {}</h1>
    </div>
  );
};

export default UpdateUser;
